# Images

Required file:

```text
images/background.png
```

Use a 320×240 PNG for best results. The YAML also has `resize: 320x240`, but a correctly cropped 320×240 image avoids letterboxing or unexpected offsets.
